# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
on:
  push:
    branches:
      - master

name: "Flutter build CI"
jobs:
  build:
    name: Build & Release
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v1
        with:
          flutter-version: "3.7.10"
      - run: flutter pub get
      - run: flutter build apk --split-per-abi --flavor dev -t lib/app/flavors/app_dev.dart

      - name: Push to Releases
        uses: ncipollo/release-action@v1
        with:
          artifacts: "build/app/outputs/apk/release/*.apk"
          tag: v1.0.${{ github.run_number }}
          token: ${{ secrets.TOKEN }}
# push to master, main, develop
# pull request on main master


# name: bloc_ci

# on:
#   push:
#     branches: [ "master", "pranay" ]
#   # pull_request:
#   #   branches: [ "master" ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v3

#       # Note: This workflow uses the latest stable version of the Dart SDK.
#       # You can specify other versions if desired, see documentation here:
#       # https://github.com/dart-lang/setup-dart/blob/main/README.md
#       # - uses: dart-lang/setup-dart@v1
#       - uses: dart-lang/setup-dart@9a04e6d73cca37bd455e0608d7e5092f881fd603

#       - uses: actions/setup-java@v1
#         with:
#           java-version: '12.x'


#       - uses: subosito/flutter-action@v1
#         with:
#           flutter-version: '3.7.10'
      
          
        
#       - name: Running doctor
#         run: flutter doctor
        
#       - name: Install dependencies
#         run: flutter pub get
      

#       # - name: Intall flutter packages
#       #   run: flutter pub get

#       # Uncomment this step to verify the use of 'dart format' on each commit.
#       # - name: Verify formatting
#       #   run: dart format --output=none --set-exit-if-changed .

#       # Consider passing '--fatal-infos' for slightly stricter analysis.
#       - name: Analyze project source
#         run: dart analyze
      

#       # Your project will need to have tests in test/ and a dependency on
#       # package:test for this step to succeed. Note that Flutter projects will
#       # want to change this to 'flutter test'.
#       - name: Run tests
#         run: flutter test
  
#   build_android:
#     name: Build Flutter (android)
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - uses: actions/setup-java@v1
#         with:
#           java-version: '12.x'
#       - uses: subosito/flutter-action@v1
#         with:
#           flutter-version: '3.7.10'
#       - run: flutter pub get 
#       - name: Creating build
#         run: flutter build apk --debug --split-per-abi --flavor dev -t lib/app/flavors/app_dev.dart
#       - uses: ncipollo/release-action@v1

#         with: 
#             artifacts: "build/app/outputs/apk/debug/*"
#             token: ${{ secrets.TOKEN }}
#             tag: v1.0.${{github.run_number}}  


#       - name: Upload app apk artifact
#         uses: actions/upload-artifact@v2
#         with:
#               name: apk
#               path: "build/app/outputs/apk/debug/*"

#   # build_ios:
#   #   name: Build Flutter (android)
#   #   runs-on: ubuntu-latest
#   #   steps:
#   #     - uses: actions/checkout@v3
#   #     - uses: actions/setup-java@v1
#   #       with:
#   #         java-version: '12.x'
#   #     - uses: subosito/flutter-action@v1
#   #       with:
#   #         flutter-version: '3.7.10'
#   #     - run: flutter pub get 
#   #     - name: Creating build
#   #       run: flutter build ipa --flavor dev -t lib/app/flavors/app_dev.dart
#   #     - uses: ncipollo/release-action@v1
#   #       with: 
#   #           artifacts: "build/app/outputs/apk/debug/*"
#   #           token: ${{ secrets.TOKEN }}
#   #           tag: v1.0.${{github.run_number}}        

      
    
